{
  "mcpServers": {
    "spice-sql": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/bundle.js"],
      "env": {
        "SPICE_CLOUD_API_KEY": "${SPICE_CLOUD_API_KEY}"
      }
    }
  }
}
